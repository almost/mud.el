Requires lui.el whic ships with [Circe](http://www.emacswiki.org/emacs/Circe)

To run stick mud.el in your elisp path and add `(require 'mud)` to your .emacs. You can then use `M-x mud` to connect to a mud.
